<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Drinks Index</title>
</head>
<body>
    <h1>Welcome to gitPub</h1>
        <ul>
            <% for(let i = 0; i < allDrinks.length; i++) { %>
            <% allDrinks[i].name.charAt(0).toUpperCase() %>
                <li>
                    <a href="/drinks/<%=i%>"><%=allDrinks[i].name %></a>
                </li>
            <% } %>
        </ul>
</body>
<style type="text/css">
    body {
        color: blanchedalmond;
        background-color: steelblue;
    }
</style>
</html>

<!-- + allDrinks[i].slice(1) = allDrinks.join(" ") -->